rustler::atoms! {
    ok,
    error,

    // errors
    answer_error,
    candidate_error,
    invalid_atom,
    invalid_json,
    invalid_local_description,
    invalid_remote_description,
    invalid_track,
    lock_fail,
    not_found,
    offer_error,
}

//pub(crate)
// use self::*;
